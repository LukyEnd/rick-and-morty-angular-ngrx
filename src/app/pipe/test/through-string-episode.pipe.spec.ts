import { ThroughStringEpisodePipe } from '../through-string-episode.pipe';

describe('ThroughStringEpisodePipe', () => {
  it('create an instance', () => {
    const pipe = new ThroughStringEpisodePipe();
    expect(pipe).toBeTruthy();
  });
});
