import { TranslatorsMonthPipe } from '../translators-month.pipe';

describe('TranslatorsMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatorsMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
