import { TranslatorsBrPipe } from '../translators-br.pipe';

describe('TranslatorsBrPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatorsBrPipe();
    expect(pipe).toBeTruthy();
  });
});
